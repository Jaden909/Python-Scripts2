I haven't built the game as an .exe yet. to play it in its current state, open the code workspace file in VS code and run the code of the open file. Alternatively, You could also open the python file directly with VS code and then run it.

